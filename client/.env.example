# Optional: full URL to Laravel API (e.g. https://api.yoursite.com).
# Leave unset during Vite dev to use the proxy in vite.config.js (same-origin /api → 127.0.0.1:8000).
# VITE_API_URL=http://127.0.0.1:8000
